# 构建上下文排除项
#
# 为什么必须有这个文件：Dockerfile 里是 `COPY . .`，
# 没有本文件时会把下面这些全部送进构建上下文并打进镜像层 ——
# 其中最严重的是 .env（含 DEEPSEEK_API_KEY，会被永久留在镜像层里，
# docker history / docker save 可以翻出来）。
#
# 背景见 doc/RAG知识库设计.md §10.3

# 本地虚拟环境（上万文件，且与容器内依赖冲突）
.venv/

# 运行时日志（由 compose 的 ./logs 卷挂载提供）
logs/

# 密钥 —— 改为在运行时用 compose 的 env_file 注入
.env

# Python 缓存
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 版本控制与 IDE
.git/
.gitignore
.idea/
.vscode/

# 构建产物本身（不需要进镜像）
Dockerfile
.dockerignore
docker-compose.yml
README.md

# 测试夹具
test-*.json

# 注意：doc/ 与 scripts/ 保留 —— RAG 入库脚本需要读 doc/ 下的语料。
